# Атлас Аллодов — роадмап (актуализировано)

_Собрано из: старого `atlas-roadmap.md` (что осталось незакрытым), ручного
тестирования (`отчет_020926`), внешнего AI-аудита (`аудит_атлас_АО`) и
живого прогона тестов (`тесты020926.txt`)._

## Статус на сегодня

- **Бэкенд: 276/276** тестов зелёных (живой прогон, node:test).
- **П0 закрыт** (оба пункта, 0 и 1, — см. ниже). Живой Playwright-прогон
  с браузерами ещё не проводился в этой сессии (нет доступа к CDN
  Playwright для загрузки браузерных бинарников) — корневая причина
  найдена и исправлена, проверено без браузера через jsdom + реальный
  локальный сервер. Следующий логичный шаг — прогнать `test-e2e.sh` живьём
  на своей машине и убедиться, что весь сьют теперь стартует; если
  какие-то тесты всё ещё падают — уже по существу, не из-за П0.
- Из старого роадмапа три пункта остались непроверенными «живьём» и
  переносятся сюда (раздел «Перенесено из старого роадмапа»).
- Раздел «Критично» полностью закрыт (п.12 и п.14 — оба).

---

## 🔴 П0 — блокирует всё остальное тестирование

0. **~~Нерешённый кластер: `#wikiView`/`#sourcesView`/`#timelineView` не
   получают класс `show` после клика по разделам вики~~ — исправлено.**
   Причина найдена: `mapView.js` (подключается в `index.html` раньше
   `wikiView.js`) на верхнем уровне содержал прямую ссылку на функцию
   `addEventListener('click', showWiki)` — она резолвится немедленно, в
   момент выполнения скрипта, а не по клику. В этот момент `showWiki` ещё
   не определена (её скрипт подключается позже) → `ReferenceError`,
   обрывающий выполнение всего ОСТАЛЬНОГО `mapView.js`, включая цикл
   чуть ниже, который вешает обработчики на все 4 пункта выпадающего
   меню «Вики ▾». Поэтому ни один из разделов не открывался вообще ни
   при каких условиях — не баг конкретно в `showWiki()`, CSS или
   делегировании кликов, как предполагалось раньше, а обрыв скрипта на
   уровне регистрации обработчиков. Исправлено оборачиванием в
   `()=>showWiki()` (резолвит имя по клику, когда все скрипты уже
   загружены). Проверено без браузера — jsdom + реальный локальный
   сервер, `ReferenceError` пропал, `#wikiView` получает класс `show`.
   Проверка реальным Playwright-прогоном (с браузерами) — следующий шаг,
   недоступно в среде без выхода к CDN Playwright.

1. **~~`e2e/global-setup.js` логинится как `admin/admin0000`, а сервер
   теперь генерирует случайный bootstrap-пароль~~ — уже исправлено в
   этой сборке, роадмап не был актуализирован.** Проверено по коду:
   `server/db.js` поддерживает `ATLAS_BOOTSTRAP_PASSWORD` для
   тестового окружения, `e2e/playwright.config.js` его выставляет
   (`e2e-bootstrap-password-123`), `e2e/constants.js` содержит то же
   значение, `global-setup.js` логинится именно им. Раздел
   «Как двигаться» ниже ссылался на этот пункт как на блокер — это уже
   неактуально, единственным реальным блокером был п.0 выше.

---

## Перенесено из старого роадмапа (незакрытое)

2. **~~Второй, независимый блокер e2e: rate-limiter по IP валит почти весь сьют~~ — исправлено.**
   `server/security/rateLimiter.js` блокирует IP на 5 минут после 5
   неудачных попыток входа, лимит общий на весь процесс сервера и не
   сбрасывается между тестами. Сьют специально гоняет несколько тестов
   «неверный пароль» по всему прогону — как только с одного и того же
   `localhost` набралось 5 фейлов суммарно, **все последующие тесты**,
   которым нужен реальный логин (даже с верным паролем), стабильно
   валятся таймаутом на `actionTimeout` (~10-11с) — картина «первый тест
   зелёный, дальше всё красное», подтверждено логом реального прогона
   (228 тестов, 1 воркер). Добавлен `ATLAS_DISABLE_RATE_LIMIT=1` в
   `webServer.env` — только для e2e, в проде переменная не ставится,
   поведение блокировки не меняется.
   - **Отдельно стоит рассмотреть как прод-риск** (не блокер, но стоит
     занести в раздел «Критично» выше): блокировка по IP, а не только по
     аккаунту, значит один сотрудник, ошибившийся паролем 5 раз с общего
     офисного/NAT-адреса, блокирует на 5 минут вход **всем** остальным с
     того же IP. Стоит подумать, не сделать ли лимит только по аккаунту
     (`attemptsByTarget`), либо увеличить `MAX_ATTEMPTS` для IP-ключа
     отдельно от аккаунтного.

3. **Не проверено вживую: фикс `authOverlay`/scrypt.async** — ожидание,
   что зависание `#authOverlay` в `auth.spec.js`/`dragdrop.spec.js` и
   гонка `config.spec.js` пропали после перевода `scryptSync` → `scrypt`
   async. Требует живого e2e-прогона (блокировано П0).
4. **Топбар на мобильном экране** — после схлопывания Вики-меню в
   выпадашку не проверялось живьём, помещается ли оставшееся (переключатель
   проекта, поиск, 2 фильтра, тема, редактор, вход) на мобильном экране.
5. **`:focus-visible` по всему сайту** — правило добавлено, но реальная
   Tab-навигация в браузере не проверялась вживую.
6. **`playwright install chromium webkit`** в CI/на чистой машине — не
   проверено вживую (нет доступа к GitHub Actions из текущей среды
   разработки).

---

## 🔴 Критично — до публичного релиза

7. **~~CSRF-защита отсутствует~~ — исправлено.** Куки уже были
   `sameSite:'lax'` (блокирует большинство межсайтовых POST в современных
   браузерах), добавлен второй, независимый слой — мидлварь на все
   `/api/*` не-GET/HEAD/OPTIONS запросы сверяет `Origin` (или `Referer`,
   если браузер не прислал `Origin`) с собственным хостом сервера и
   отдаёт 403 при несовпадении; запросы вовсе без обоих заголовков
   (curl, серверные клиенты — CSRF там невозможен в принципе) не
   блокируются. Покрыто новым `server/test/csrf.test.js`.
   - **Заодно нашлись и исправлены два независимых реальных бага** при
     прогоне полного бэкенд-сьюта после этой правки:
     1. `server/security/rateLimiter.js` ссылался на необъявленную
        константу `MAX_TRACKED_KEYS` — это роняло **любую** попытку
        входа с неверным паролем в необработанное исключение (500
        вместо 401), то есть блокировка по IP/аккаунту (roadmap-пункт
        про 5 попыток) фактически не работала вообще ни разу, тихо
        падая при первой же регистрации неудачной попытки. Похоже на
        недоделанный более ранний фикс пункта №33 (лимит размера Map) —
        сама защита от роста была на месте, а константа для неё —
        нет.
     2. **`secure:true` по умолчанию (см. п.9 ниже) ломал set-cookie на
        обычном HTTP** — что и есть протокол, по которому ходят бэкенд-
        тесты. `ATLAS_ALLOW_HTTP=1` был добавлен ранее только в
        e2e-конфиг, но не в сами тестовые файлы бэкенда: с момента фикса
        п.9 и до этого прогона все 26 файлов бэкенд-тестов, где идёт
        логин, были тихо сломаны (сессионная кука никогда не
        выставлялась) — просто никто не гонял `npm test` целиком в
        промежутке, чтобы это заметить. Добавлена `ATLAS_ALLOW_HTTP=1` во
        все 26 файлов. После обоих фиксов — 261/261 бэкенд-тестов зелёных.
8. **~~Optimistic locking по `rev` — перепроверить полноту~~ — проверено,
   пробел был реальный, закрыт частично.** Аудит оказался прав, не
   ложное срабатывание: сервер (`routes/allods.js`) проверяет
   `expectedRev` корректно и единообразно для всего PATCH-запроса, когда
   поле передано — но на фронтенде `expectedRev` реально отправляли
   только текстовые поля через `patchWithConflict()` в `detailView.js`
   (описание, история и т.п.). Без защиты оставались: смена проекта,
   `icon_url`, `location_map_url` — и, что важнее всего, **перетаскивание
   маркера острова по карте (`mapX`/`mapY`)** — ровно тот сценарий, где
   два редактора чаще всего работают одновременно и тихо затирают
   правки друг друга. Добавил `expectedRev` в оба места
   `mapView.js`, где сохраняется позиция (перетаскивание уже
   размещённого маркера и бросок из лотка неразмещённых) — при 409
   локальная позиция откатывается на актуальную с сервера с тостом,
   не выдаётся полноценный UI выбора конфликта (как у текстовых полей),
   этого достаточно, чтобы не расходиться визуально с сохранённым.
   `icon_url`/`location_map_url`/смена проекта — сознательно не трогал в
   этом заходе, это отдельная, менее рискованная задача. Добавлено
   покрытие `server/test/optimistic-locking.test.js` (обратная
   совместимость без `expectedRev`, конфликт на текстовых полях,
   конфликт конкретно на `mapX`/`mapY`, конфликт на локациях).
9. **~~HTTPS enforcement по умолчанию~~ — исправлено.** `secure` теперь
   `true` по умолчанию, `ATLAS_ALLOW_HTTP=1` — явный опт-аут для локальной
   разработки/e2e без HTTPS. Все штатные способы запуска (start.sh/.bat,
   оба docker-compose) уже ставят `ATLAS_HTTPS=1` и не задеты; e2e получил
   `ATLAS_ALLOW_HTTP=1` в `playwright.config.js`.
10. **~~Открытый JSON export без фильтра `allowedProjects`~~ — не баг,
    закрыто.** Проверил и уточнил у автора: `GET /api/allods` намеренно
    без `requireAuth` — это публичная вики с несколькими под-проектами,
    переключатель проектов виден всем гостям без входа, и гости и так
    видят все острова всех проектов через тот же публичный API. Само
    поле `allowedProjects` в коде (`routes/auth.js`) и означает
    ограничение именно **прав редактирования и апрува правок** по
    ролям — не видимости контента на чтение. Аудит спутал одно с другим;
    реального пробела в правах доступа нет.
11. **~~Автобэкапа фактически нет / не срабатывает~~ — проверено, не
    баг кода.** Автобэкап (`backupScheduler.js`) выключен по умолчанию
    везде — требует явного `ATLAS_AUTO_BACKUP=1`; в `.env.example` эта
    строка закомментирована, `docker-compose.yml` пробрасывает пустое
    значение, `start.sh`/`.bat` вообще не задают эту переменную. «Бэкап
    не делался за 2 часа» — ожидаемое поведение при не включённой
    настройке, а не регрессия. Ручной бэкап (`GET /api/backup/download-
    full`) проверил напрямую end-to-end (логин админом → запрос →
    получен корректный .zip, 200, верные заголовки) — работает штатно;
    он скачивается в браузер (`window.location.href`), а не кладётся на
    сервер в `backups/` — если в ручном тесте смотрели именно в
    серверную папку, оттуда и путаница. Если всё же нужно, чтобы
    автобэкап был включён по умолчанию «из коробки» — это отдельное,
    осознанное решение о дефолтах, а не баг.
12. **~~После восстановления из полного архива сайт зависает~~ —
    исправлено.** Добавлен `server/restart.js`: после `/backup/restore` и
    `/backup/restore-full` процесс сам порождает detached-копию себя
    (тот же `argv`/env) перед выходом — новая копия поднимается и
    переоткрывает БД за секунду-две, ручной перезапуск больше не нужен.
    Под Docker respawn намеренно отключён (там это уже делает
    restart-policy контейнера, второй процесс на порту дал бы гонку).
    Проверено вживую на реальном сервере (не только через
    `ATLAS_TEST_NO_EXIT`): PID процесса меняется, сайт отвечает и логин
    работает уже в новой копии в течение секунды после ответа API.
13. **~~Пароль можно сменить на такой же~~ — исправлено.** `/api/auth/password`
    теперь сверяет `newPassword` напрямую с реальным хэшем (а не только со
    строкой `currentPassword` — это работает и в ветке принудительной
    смены, где `currentPassword` вообще не передаётся) и отдаёт 400, если
    новый пароль совпадает со старым. Задело `global-setup.js` (он менял
    пароль редактора на такой же самый, чтобы снять `must_change_password`
    без лишнего трения) и один бэкенд-тест в `auth.test.js` — оба
    переведены на паттерн «временный пароль → назад» с реальной сменой.
14. **~~Самостоятельный сброс пароля через UI не реализован~~ —
    исправлено.** Выбран второй из двух вариантов, предложенных в этом же
    пункте: запрос на сброс, который админ подтверждает через панель
    «Настройки» (без email — сервер не поднимает исходящую почту).
    Кнопка «Забыли пароль?» на экране входа теперь реально отправляет
    запрос (`POST /api/auth/reset-request`, публичный, не палит
    существование аккаунта). Админ видит список в «⚙ Настройки → 👤
    Пользователи» (новая карточка) и подтверждает — сервер генерирует
    новый временный пароль, показывает его админу один раз, обрывает
    старые сессии аккаунта; вход новым паролем сразу требует смены
    (`must_change_password`). 10 новых тестов + полный бэкенд-сьют:
    276/276. Проверено и вживую на реальном сервере (curl): старый пароль
    после approve отклоняется, новый работает и требует смены при входе.

---

## 🟠 Важно — Sprint 1

15. **Вероятно уже исправлено вместе с П0 — требует финальной живой
    проверки.** «Кнопки во всех вкладках не кликаются» (отчёт, пункт 6, со
    скриншотом, который у меня нет). Сделал систематический аудит: прошёл
    по каждому месту в коде, где обработчик клика передаётся в
    `addEventListener` прямой ссылкой на функцию (не через `()=>...`), и
    проверил, что функция либо объявлена в том же файле (hoisting решает
    порядок), либо в файле, подключённом РАНЬШЕ — то есть ни одного
    другого места с тем же классом бага, что и в П0 (`mapView.js` →
    `showWiki`), не нашёл. Рабочая гипотеза: «вкладки» из отчёта — это
    как раз разделы вики (Атлас/Источники/Хронология/Архипелаги), которые
    неспециалист вполне мог назвать «вкладками», а не «пунктами меню» —
    и тогда это ровно П0, уже закрытый в v26w38-1. Не подтверждаю на
    100% без самого скриншота — стоит перепроверить живым кликаньем по
    сайту (в первую очередь в режиме редактора, раз в отчёте отмечено
    отдельно).
16. **Исправлено.** Корень был глубже, чем звучало в отчёте: в
    `index.html` все статические пути (`<script src="js/...">`,
    `<link href="style.css">`, favicon, фоновая карта) были
    ОТНОСИТЕЛЬНЫМИ, и точно так же логотипы в `PROJECTS[].logo`
    (`projects.js`). Без `<base>` относительный путь резолвится от
    ТЕКУЩЕГО url страницы, а не от корня сайта. На плоских путях (`/`,
    `/map`, `/wiki` — один сегмент) это случайно совпадало с корнем и
    работало; но на `/allod/:id/:slug` (страница острова — два сегмента)
    `assets/...` резолвился как `/allod/:id/assets/...` — 404.
    Статически это било бы по ВСЕЙ странице при прямом заходе/обновлении
    на url острова (все скрипты/стили тоже относительные!) — но
    SPA-навигация не перезагружает уже подключённые скрипты, поэтому по
    факту ловилось только то, что рендерится ЖИВЬЁМ прямо во время
    пребывания на странице острова — переключатель проектов, который
    перерисовывается при клике на другой проект. Отсюда и жалоба именно
    про «иконки слетают на странице острова, восстанавливаются после
    повторного клика» — повторный рендер случался уже на другом,
    однослойном url. Добавлен `<base href="/">` в `<head>` — резолвит
    все относительные пути в документе от корня раз и навсегда,
    независимо от глубины url; точечно чинить каждый файл было бы
    хрупко к следующему случайно добавленному относительному пути.
    Проверено: Node URL-резолюшен (до/после), реальный сервер + jsdom на
    `/allod/a001/avilon-1` (все скрипты/стили/иконки резолвятся в
    `/assets/...`, `/js/...`, не в `/allod/a001/...`), плюс аудит всех
    `href="#"`-ссылок в кодовой базе на `preventDefault()` — `<base>` не
    создаёт новых багов навигации. 276/276 backend (фронтенд-фикс, но
    прогнал для порядка).
17. **Частично исправлено.** Кнопка «✕ Удалить остров» (`.del-allod`)
    была захардкожена на непрозрачный тёмный фон (`rgba(11,14,26,0.55)`),
    из-за чего в светлой теме получалась нечитаемая комбинация (текст уже
    переключался на тёмный цвет светлой темы, фон — нет) — тот же класс
    бага, что и у `#topbar`/`#legalFooter` (см. существующий комментарий в
    CSS про баг из живого фидбека) и что и в п.18 ниже. Добавлен
    light-theme override, тем же паттерном. Цветовая часть жалобы (пункт
    21, светлая тема) должна быть закрыта. «Криво накладывается» в тёмной
    теме (пункт 16) — НЕ трогал: это про позиционирование, не про цвет, и
    без скриншота не смог однозначно воспроизвести саму нестыковку
    (`position:absolute` относительно `.detail-hero`, ничего явно
    сломанного в разметке/CSS на вид) — нужна живая проверка или
    скриншот, чтобы не гадать вслепую.
18. **Исправлено.** Тот же класс бага, что и в п.17 выше, ровно в тех
    двух местах, что названы в пункте: `#tray` (лоток неразмещённых
    островов, фон был `rgba(18,23,42,0.96)`) и `.drag-ghost` (подпись у
    курсора при перетаскивании острова из лотка на карту — это и есть
    «форма вытягиваемого острова») — у обоих фон был захардкожен на
    тёмный, независимо от темы, при том что текст внутри уже был
    theme-aware (`var(--parchment)`/`var(--gold-bright)`, в светлой теме
    становятся тёмными). Добавлены `html[data-theme="light"]` overrides
    для обоих, тем же паттерном (`rgba(247,241,222,…)`), что уже
    использовался для `#topbar`/`#legalFooter`. Заодно прошёлся по всем
    остальным хардкоженным `rgba(11,14,26,…)`/`rgba(18,23,42,…)` в
    style.css — остальные (подписи маркеров на карте, кнопка удаления
    фото в галерее, затемнение под hero-картинкой острова) висят НАД
    произвольной графикой/фотографиями, а не над фоном страницы, и там
    фиксированный тёмный оттенок — скорее осознанный выбор для
    контраста, а не забытый theme override; не трогал их без явной
    жалобы в отчёте, `#legalFooter`-подобный `position:fixed` блок внизу
    (строка ~878) оказался самим `#legalFooter`, уже закрыт раньше.
19. **Проверил — похоже на ложное срабатывание аудита, не баг.**
    `GET /api/auth/users` действительно только `requireAdmin` — но это и
    есть весь список эндпоинтов, отдающих список пользователей, других
    нет (проверено грепом по всем роутам). `allowedProjects` — это
    ограничение РЕДАКТОРА на то, какие ОСТРОВА он может править, а не
    какие АККАУНТЫ он может видеть; сам эндпоинт для редакторов и так
    недоступен целиком (403, не 200 с урезанным списком). Админы по
    всему остальному коду и документации сознательно освобождены от
    `allowedProjects` («на администраторов ограничение не действует» —
    см. подсказку в «Настройки → Пользователи»), так что фильтровать
    список пользователей по `allowedProjects` для них означало бы
    сломать управление пользователями, а не исправить дыру. Если у
    аудита была в виду другая, более конкретная угроза — нужны детали,
    что именно admin не должен видеть и почему.
20. **Исправлено.** Новая таблица `audit_log` (`server/db.js`) +
    хелпер `logAudit()` (`server/audit.js`), не роняющий само действие,
    если запись лога не удалась. Залогированы все перечисленные в
    находке действия: создание пользователя (`user.create`, только
    приглашение действующим админом — bootstrap первого аккаунта не
    логируется, логировать там некого), удаление (`user.delete`), смена
    роли (`user.role_change`, с `from`/`to` в `details`), блокировка/
    разблокировка (`user.disable`/`user.enable`), публикация черновика
    (`allod.publish_draft`) — и заодно оба вида сброса пароля админом
    (`user.force_password_reset`, `user.password_reset` — approve из
    п.14), раз уж это тоже критическое действие над чужим аккаунтом.
    Просмотр — `GET /api/audit-log` (только админ, `limit` до 500) и
    read-only карточка «📋 Журнал действий» в «⚙ Настройки» (без неё
    лог был бы записан, но никто бы его не увидел — сама находка
    просила только таблицу, но лог без смотрового окна бесполезен для
    реальной цели аудита). 9 новых тестов + полный бэкенд-сьют:
    285/285.
21. **Частично исправлено.** Копнул глубже формулировки: required-поля
    технически УЖЕ проверялись на клиенте почти везде (`textPrompt`,
    `wireEditableField` уже поддерживали `required`/`requiredMsg`) — но
    при пустом поле просто ТИХО ничего не происходило: пользователь жмёт
    «Сохранить»/«Отправить», и ничего не видно — будто кнопка сломана.
    Это и есть суть находки, а не отсутствие проверки как таковой. Нашёл
    3 места с этим silent-fail: `textPrompt` (`picker.js` — общий для
    создания острова/локации/проекта/ссылок на иконки, самый
    высокорычаговый фикс — одно место чинит все его вызовы разом),
    форма источника (`sourcesView.js`) и форма «Сообщить об ошибке»
    (`detailView.js`, читательская, не только для редакторов). Добавлен
    общий хелпер `flashFieldInvalid()` (`state.js`) — подсвечивает поле
    (рамка + короткая тряска, `.field-invalid` в style.css), возвращает
    фокус, показывает тост с текстом ошибки. `wireEditableField` НЕ
    трогал — там уже был тост (не тихо), просто без тряски; другое
    поведение при пустом (сброс к предыдущему значению, а не остаться
    редактировать) — сознательно другой, тоже рабочий флоу, менять не
    стал. Проверено вживую через jsdom: пустой submit — модалка остаётся
    открытой, поле подсвечено, фокус на нём; валидный submit — закрывается
    как раньше. 285/285 backend (фронтенд-фикс, для порядка). Что
    ОСТАЁТСЯ открытым: это фикс видимости отказа, не полноценная
    валидация формата (URL, длина, диапазоны дат и т.п.) — если у
    находки BUG-009 была в виду ещё и она, нужны подробности, какие
    именно поля и какой формат проверять.
22. **Частично исправлено.** Разобрался: «Сообщить об ошибке» (текстовое
    обращение читателя) на самом деле УЖЕ полностью реализовано на
    бэкенде и уже отображалось в «⚙ Настройки» (`server/routes/reports.js`
    + карточка в `settings.js`) — не хватало не самой видимости, а
    ПРИГЛАШЕНИЯ туда зайти: ничто на сайте не намекало админу, что там
    вообще что-то накопилось, кроме привычки самому периодически
    заглядывать. Добавлен бейдж с числом открытых обращений прямо на
    кнопке «⚙ Настройки» (`refreshReportBadge()` в `authUI.js`,
    `data-report-count` + CSS `::after`) — обновляется при входе и сразу
    после «Отметить решённым»/«Удалить» в самой панели. Проверено вживую
    через jsdom + реальный сервер: анонимный POST `/api/reports`, вход
    админом — бейдж появляется с правильным числом и title. 285/285
    backend (фронтенд-фикс, для порядка). Что ОСТАЁТСЯ открытым — сама
    более крупная задумка из отчёта/аудита: **полноценная возможность
    гостя предложить правку** (не просто текстовый отчёт, а реальное
    изменение полей острова), которая падает в модерацию, объединённую
    с лентой «последние изменения», со значком «требует проверки» и
    подсветкой изменённых полей при просмотре. Это отдельная, гораздо
    более крупная фича — новый анонимный write-путь (нужна серьёзная
    защита от злоупотреблений, по аналогии со строгим rate-limit'ом у
    `/reports`), плюс UI, которого пока нигде в проекте похожего нет.
    Не начинал — нужно сперва решить архитектуру (как гость редактирует:
    те же contenteditable-поля без логина, или отдельная форма «предложить
    правку»?) и лучше — с визуальной обратной связью, тут вслепую гадать
    рискованно.
23. **Не найден общий раздел редактирования фракций/архипелагов и т.д.**
    (отчёт, пункт 19) — либо доделать UI, либо это баг навигации.
24. **Раздел «Пользователи» нечитаемый, «как каша»** — нужна структура
    (отчёт, пункт 27).
25. **Черновики: недостаточно явно показано, что есть неопубликованные
    изменения.** Плашка «ЧЕРНОВИК» должна быть закреплена при скролле,
    всегда на виду (отчёт, пункты 15 и 17). Плюс: изменение картинок в
    режиме черновика не считается «что сохранять» — баг, картинка тоже
    должна попадать в черновик как остальные поля.
26. **Хронология: клик по году не переводит в общую хронологию на эту
    дату** (отчёт, пункт 14). Плюс поддержка **месяца и дня**, не только
    года, в датах хронологии (отчёт, пункт 4).
27. **World Timeline UI отсутствует** — таблица `timeline_events` со
    `scope='world'` существует, но нет визуализации мировой хронологии
    как отдельного раздела вики (аудит, часть V).
28. **Auto-save черновика** — при потере вкладки/краше браузера
    несохранённые правки в черновике теряются (аудит, часть V).
29. **Предупреждение о параллельном редактировании черновика** другим
    редактором (аудит, часть V) — расширение п.7/21.
30. **2FA (Google Authenticator) + привязка e-mail к аккаунту** — прямой
    запрос пользователя (отчёт, пункт 12).
31. **Механизм отката (revert) правки на сервере отсутствует** — есть
    только просмотр истории (аудит, BUG старой части II, п.7).

---

## 🟡 Средне — Sprint 2

32. **Path traversal в `deleteUploadedFile()`** — сузить проверку через
    `path.relative()`/`resolve()` внутри `UPLOAD_DIR` (аудит, BUG-003).
33. **Нет timeout на обработку изображений** (`sharp` в `upload.js`) —
    может повиснуть запрос (аудит, BUG-004/BUG-010).
34. **Rate-limiter потенциальный memory leak** — `Map` в
    `rateLimiter.js` без явного предела размера (аудит, BUG-007).
35. **Нет логирования ошибок `ROLLBACK`** в транзакциях `db.js`
    (аудит, BUG-008).
36. **`contenteditable` не чистится от HTML при paste** (аудит, BUG-011).
37. **Нет UNIQUE-ограничения на (project, name) архипелагов** (аудит,
    BUG-014).
38. **Дублирование информации о зависимостях недостаточно понятно
    пользователю** — «что мне делать с инфой по зависимостям, как их
    поднимать?» (отчёт, пункт 13) — нужна понятная инструкция/кнопка,
    а не просто вывод статуса.
39. **Форма источника требует ссылку всегда** — не учитывает случаи
    файлов игры, удалённого контента, ответов на мероприятиях. Нужна
    форма с выбором типа источника (ссылка / текстовое описание,
    книга-автор-страницы) (отчёт, пункт 24).
40. **Обязательная картинка фракции** — сделать генератор геральдического
    щита/эмблемы как альтернативу загрузке (отчёт, пункт 25) — пользователь
    готов накидать элементов для генератора.
41. **Форма редактора: выпадающий список + кнопка «новый»** — везде, где
    задан набор вариантов; право на «новый» — у редактора и админа (отчёт,
    пункт 3).
41а. **~~Создание нового значения через поисковый пикер теряло регистр~~ —
    исправлено.** `renderPickerList()` в `picker.js` сравнивал варианты без
    учёта регистра (правильно), но по ошибке использовал ту же
    приведённую к нижнему регистру строку и как **само создаваемое
    значение** — любая новая категория/фракция/и т.п., набранная через
    этот пикер (например, «Испытательный полигон E2E»), сохранялась
    целиком в нижнем регистре. Нашлось при e2e-прогоне. Теперь для
    создания используется исходный ввод пользователя, для поиска/сравнения
    — по-прежнему без учёта регистра.
42. **Правый клик при размещении украшений** должен позволять перемещение
    зажатой мышкой по карте, а не ставить объект (отчёт, пункт 7).
43. **Кнопка «прикрепить новый источник»** — поднять наверх, под форму
    ввода (отчёт, пункт 5).
44. **Фильтр по дате изменения** в разделе последних изменений (отчёт,
    пункт 9); также убрать «Описание», оставить только «Последние
    изменения» (пункт 10).
45. **Кнопки в разделе «О системе» не показывают вес/размер** — указывать
    рядом с помеченными кнопками (отчёт, пункт 32); размер БД должен
    учитывать картинки/графику, а не только саму базу (пункт 31).
46. **Кнопка скачать сторонние лицензии** — сами лицензии уже есть, нужна
    кнопка (отчёт, пункт 29).
47. **Из настроек — кнопка перехода в «О системе»** (отчёт, пункт 30).
48. **Полнотекстовый поиск (FTS)** — сейчас только по названию острова,
    нужен по описанию/локациям/истории (аудит, часть III).
49. **Система тегов/категорий** для островов помимо фракции (аудит).
50. **Diff-просмотр черновика vs live** — версия истории есть, но нет
    наглядного постраничного сравнения полей (аудит, часть V).

---

## 🎨 Низкий приоритет / полировка — Sprint 3+

51. **Feedback-анимации на кнопках** (`:active { transform: scale(0.98) }`,
    transition) — сейчас клики визуально «немые» (аудит).
52. **Skeleton-лоадеры** вместо пустого состояния при загрузке (аудит).
53. **Overscroll/позиционирование** выпадашки поиска на маленьких экранах
    (аудит).
54. **Сохранение позиции карты** в localStorage между визитами (аудит,
    BUG-015).
55. **Smooth scroll** (аудит, BUG-016).
56. **Fullscreen-режим лайтбокса** (аудит, BUG-017).
57. **Система комментариев читателей** на страницах островов (аудит,
    низкий приоритет по факту, т.к. требует модерации — но давно
    просят).
58. **Лайки/рейтинг/избранное** для читателей (аудит).
59. **Документированный OpenAPI/Swagger** для существующих JSON-эндпоинтов
    (аудит).
60. **Уведомления об обновлениях** по категориям (аудит).
61. **PWA/offline-режим** (аудит) — низкий приоритет, отдельно от
    основной вики-логики.
62. **Интеграция с MediaWiki API** (аудит) — под вопросом, нужна ли вообще.

---

## Не подтверждено / требует ручной проверки перед тем как заносить в баг-трекер

- **Пункт 18 отчёта** («при клике в этих местах должно ходить по вики, а
  перекидывает на карту») — нужен скриншот/URL, к какому именно разделу
  относится клик, чтобы не путать с уже закрытым пунктом 1 старого
  роадмапа (URL-роутинг). Возможно, отдельный частный случай навигации.
- **SQL injection «потенциал»** (аудит, «Уязвимость» №1, CVSS 7.5) —
  аудит сам отмечает, что защита `if(!toKill.length) return;`, похоже,
  уже стоит; реальной уязвимости не подтверждено, скорее чек-лист на
  regression-тест, а не открытый баг.
- **Потенциальная XSS в `parseProse()`** (аудит, старая часть II, п.2) —
  аудит сам же пишет «баг не найден, код корректен». Оставлено здесь
  только как пункт для regression-теста, не как открытая задача.

---

## 🧪 Покрытие тестами — отдельный системный пункт

Регрессы последних сессий почти все живут на стыке фронтенда и реального
UI-потока, там, где e2e либо не существует, либо проверяет только
happy-path. Пока П0 не закрыт, ни один e2e-тест не подтверждён живым
прогоном вообще — часть этих дыр могла существовать уже давно незамеченной.

- **Нет `e2e/tests/drafts.spec.js`** — `server/test/drafts.test.js`
  покрывает API, но ни один e2e-тест не гоняет реальный поток черновика
  через UI. Отсюда пропущенные регрессы: удаление скриншота в режиме
  черновика не считается изменением («нечего сохранять»), плашка
  «ЧЕРНОВИК» не закреплена при скролле. Самый приоритетный пробел —
  учитывая объём находок именно здесь (см. п.25 выше).
- **Нет `e2e/tests/theme.spec.js`** — переключение светлая/тёмная тема
  не тестируется e2e вообще, при этом минимум 3 находки именно на теме
  (кнопка удаления острова, «неразмещённые острова», форма острова —
  п.17/18 выше).
- **Нет e2e на бэкап через UI** — `backup-*.test.js` гоняют бэкап
  напрямую через API/scheduler, кнопку «сделать бэкап» из настроек никто
  не кликает. Отсюда живой баг «бэкап не делался» невидим для сьюта
  (п.11 выше).
- **Нет e2e на сброс пароля** — отдельная кнопка не покрыта ни одним
  тестом (п.14 выше).
- **Нет общего smoke-теста** «пройтись по всем вкладкам и покликать
  видимые кнопки» — баг «кнопки не кликаются во всех вкладках» (п.15)
  в принципе не ловится точечными тестами по фичам, нужен отдельный
  обходной smoke-прогон.
- **Нет проверки устойчивости состояния при навигации** — иконки
  проектов слетают при переходе остров→вики и восстанавливаются после
  повторного клика (п.16). Под класс багов «состояние теряется между
  вью» сейчас нет вообще никакого паттерна тестов.
- **Нет `e2e/tests/decorations.spec.js`** — только `server/test/
  decorations.test.js`. Баг с правым кликом при размещении украшений —
  чисто фронтендовый (мышь/драг), бэкенд-тест его в принципе не поймает.

**Порядок действий**: сначала П0 (без него все e2e-тесты, включая новые,
не запускаются), потом — `drafts.spec.js` и `theme.spec.js` как
наивысший приоритет (больше всего необнаруженных регрессов), остальное —
по ходу Sprint 1.

## Как двигаться

1. ~~Сначала П0~~ — закрыт (см. выше). Осталось подтвердить живым
   Playwright-прогоном на машине с доступом к браузерным бинарникам.
2. ~~Дальше «Критично»~~ — закрыт полностью (п.12 и п.14).
3. Дальше «Важно» — тут много пересекающихся тем (черновики+модерация
   правок читателей — фактически одна большая задача, п.22/25/28/29).
4. «Средне» и «Полировка» — по желанию, не блокируют публичный релиз.
